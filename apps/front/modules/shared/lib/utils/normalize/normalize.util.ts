/**
 * Нормализация email и телефона.
 *
 * Реализация живёт в пакете сущностей: ею пользуются и модельный слой, и
 * интерфейс, и бэкенд. Здесь только реэкспорт, чтобы не плодить вторую копию
 * правил и не ломать существующие импорты из этого файла.
 */
export { stripInvisible, normalizeEmail, normalizePhone } from '@alfa/entities';
