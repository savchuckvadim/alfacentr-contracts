/**
 * Бюджет ожидания того, что битрикс дожует документы:
 * сначала pdf у счета с печатью, потом заполнение полей сделки файлами.
 * Генерация идет в bull-воркере без job timeout, поэтому длинное ожидание безопасно.
 */
export const DOCUMENT_WAIT_TIMEOUT_MS = 5 * 60 * 1000;

/** пауза между опросами битрикса */
export const DOCUMENT_WAIT_INTERVAL_MS = 15_000;

/** пауза после ошибки запроса — тоже считается в общий бюджет */
export const DOCUMENT_WAIT_ERROR_INTERVAL_MS = 10_000;
