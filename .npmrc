# Flatten React types to the root node_modules so TS/Next don't reference
# nested .pnpm/@types+react@<other> paths (fixes "cannot be named without reference to ..." on CI).
public-hoist-pattern[]=*@types/react*
public-hoist-pattern[]=*@types/react-dom*
